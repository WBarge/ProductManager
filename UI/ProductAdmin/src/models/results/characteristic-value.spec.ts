import { CharacteristicValue } from './characteristic-value';

describe('CharacteristicValue', () => {
  it('should create an instance', () => {
    expect(new CharacteristicValue()).toBeTruthy();
  });
});
